local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local VIM = game:GetService("VirtualInputManager")

local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- Limpieza de versiones anteriores
if CoreGui:FindFirstChild("V67_Master_UI") then
    CoreGui.V67_Master_UI:Destroy()
end

-- === CONFIGURACIÓN AIM/ESP (V67) ===
local Settings = {
    Aimbot = false, Aim360Total = false, VisibleCheck = false,
    Priority = "Mira", AimPart = "Head", TeamCheck = true,
    ShowAliados = false, ShowFOV = true, FOV = 150,
    Smoothing = 0, MaxDistance = 2000, ESP_Names = false,
    ESP_Lines = false, ESP_Chams = false, VentajaEnabled = false,
    VentajaTarget = "Cabeza", AutoShoot = false, AutoShootDelay = 0.1,
    OffX = 0, OffY = 0, OffZ = -3.5
}
local CurrentTarget = nil
local lns, nms, chams, fovLines = {}, {}, {}, {}
local Running = true
local lastShot = 0

-- === CONFIGURACIÓN SALA (V70) ===
local targetPlayer = nil
local bangActive, flingActive, spectateActive = false, false, false
local lastPos = nil 
local bangModes = {"ESPALDA", "CABEZA", "CABEZA ALTO", "PISO (ESPALDA)"}
local flingModes = {"GHOST", "TORNADO", "MISIL", "DEBAJO"}
local modeIndex, flingModeIndex = 1, 1
local ignoreTeam = false
local bangSpeed, bangTilt = 35, 35 
local flingRot = 200000 

local themeColor = Color3.fromRGB(0, 255, 150)

-- === FUNCIONES DE LIMPIEZA ===
local function removeVisuals(p)
    if lns[p] then pcall(function() lns[p]:Remove() end) lns[p] = nil end
    if nms[p] then pcall(function() nms[p]:Remove() end) nms[p] = nil end
    if chams[p] then pcall(function() chams[p]:Destroy() end) chams[p] = nil end
    pcall(function() if p.Character and p.Character:FindFirstChild("Head") then p.Character.Head.Anchored = false end end)
end

local function cleanAll()
    Running = false
    for _, l in pairs(fovLines) do pcall(function() l:Remove() end) end
    for _, p in pairs(Players:GetPlayers()) do removeVisuals(p) end
end
Players.PlayerRemoving:Connect(removeVisuals)

-- === DIBUJO FOV ===
for i = 1, 36 do
    local l = Drawing.new("Line")
    l.Thickness, l.Transparency, l.Visible = 2, 1, false
    fovLines[i] = l
end

local function updateFOVCircle()
    local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    for i = 1, 36 do
        local l = fovLines[i]
        if Settings.ShowFOV and Settings.Aimbot and not Settings.Aim360Total and Running then
            local a1, a2 = math.rad((i-1)*10), math.rad(i*10)
            l.From = center + Vector2.new(math.cos(a1)*Settings.FOV, math.sin(a1)*Settings.FOV)
            l.To = center + Vector2.new(math.cos(a2)*Settings.FOV, math.sin(a2)*Settings.FOV)
            l.Color = CurrentTarget and Color3.new(1,1,0) or Color3.new(1,1,1)
            l.Visible = true
        else l.Visible = false end
    end
end

-- === INTERFAZ MOBILE ===
local screen = Instance.new("ScreenGui", CoreGui)
screen.Name = "V67_Master_UI"

local openBtn = Instance.new("TextButton", screen)
openBtn.Size = UDim2.new(0, 45, 0, 45); openBtn.Position = UDim2.new(0, 15, 0.5, 0)
openBtn.BackgroundColor3 = themeColor; openBtn.Text = "c"; openBtn.TextColor3 = Color3.new(0,0,0)
openBtn.Font = "GothamBold"; openBtn.TextSize = 12
openBtn.Active = true; openBtn.Draggable = true
Instance.new("UICorner", openBtn).CornerRadius = UDim.new(1, 0)

local main = Instance.new("Frame", screen)
main.Size = UDim2.new(0, 250, 0, 420); main.Position = UDim2.new(0.5, -125, 0.5, -210)
main.BackgroundColor3 = Color3.fromRGB(15, 15, 15); main.BorderSizePixel = 2
main.BorderColor3 = themeColor; main.Active = true; main.Draggable = true; main.Visible = false

local header = Instance.new("Frame", main)
header.Size = UDim2.new(1, 0, 0, 35); header.BackgroundColor3 = themeColor; header.BorderSizePixel = 0
local title = Instance.new("TextLabel", header)
title.Size = UDim2.new(1, 0, 1, 0); title.BackgroundTransparency = 1; title.Text = "c2233"
title.TextColor3 = Color3.new(0,0,0); title.Font = "GothamBold"; title.TextSize = 13

openBtn.MouseButton1Click:Connect(function() main.Visible = not main.Visible end)

-- === BARRA DE CATEGORÍAS (PESTAÑAS) ===
local tabBar = Instance.new("Frame", main)
tabBar.Size = UDim2.new(1, 0, 0, 30); tabBar.Position = UDim2.new(0, 0, 0, 35)
tabBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25); tabBar.BorderSizePixel = 0

local tabLayout = Instance.new("UIListLayout", tabBar)
tabLayout.FillDirection = Enum.FillDirection.Horizontal; tabLayout.SortOrder = Enum.SortOrder.LayoutOrder

local pages = {}
local tabs = {}

local function createPage(name)
    local frame = Instance.new("ScrollingFrame", main)
    frame.Size = UDim2.new(1, 0, 1, -65); frame.Position = UDim2.new(0, 0, 0, 65)
    frame.BackgroundTransparency = 1; frame.BorderSizePixel = 0; frame.ScrollBarThickness = 3
    frame.Visible = false
    
    local list = Instance.new("UIListLayout", frame); list.SortOrder = "LayoutOrder"
    list:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        frame.CanvasSize = UDim2.new(0, 0, 0, list.AbsoluteContentSize.Y + 10)
    end)
    pages[name] = frame
    return frame
end

local function createTabBtn(name)
    local btn = Instance.new("TextButton", tabBar)
    btn.Size = UDim2.new(0.25, 0, 1, 0); btn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    btn.Text = name; btn.TextColor3 = Color3.new(1,1,1); btn.Font = "GothamBold"
    btn.TextSize = 10; btn.BorderSizePixel = 0
    
    btn.MouseButton1Click:Connect(function()
        for tName, tBtn in pairs(tabs) do tBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25); tBtn.TextColor3 = Color3.new(1,1,1) end
        for pName, pFrame in pairs(pages) do pFrame.Visible = false end
        btn.BackgroundColor3 = themeColor; btn.TextColor3 = Color3.new(0,0,0)
        pages[name].Visible = true
    end)
    tabs[name] = btn
    return btn
end

-- Crear Pestañas y Páginas
createPage("SALA"); createTabBtn("SALA")
createPage("ESP"); createTabBtn("ESP")
createPage("AIM"); createTabBtn("AIM")
createPage("VENTAJA"); createTabBtn("VENTAJA")

-- Página secundaria para opciones de jugador (SALA)
local pgSalaOpts = createPage("SALA_OPTS")
tabs["SALA_OPTS"] = Instance.new("TextButton") -- Falso tab para evitar errores

-- Activar la primera pestaña por defecto
tabs["SALA"].BackgroundColor3 = themeColor; tabs["SALA"].TextColor3 = Color3.new(0,0,0)
pages["SALA"].Visible = true


-- === COMPONENTES UI ===
local function createRow(text, parent, isToggle, stateObj, stateKey, callback, hasArrow, underlined)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(1, 0, 0, 35); btn.BackgroundColor3 = Color3.new(0,0,0)
    btn.BackgroundTransparency = 0.7; btn.Text = ""; btn.BorderSizePixel = 0
    if underlined then
        local line = Instance.new("Frame", btn)
        line.Size = UDim2.new(0.9, 0, 0, 1); line.Position = UDim2.new(0.05, 0, 1, -1)
        line.BackgroundColor3 = themeColor; line.BackgroundTransparency = 0.4; line.BorderSizePixel = 0
    end
    local lbl = Instance.new("TextLabel", btn)
    lbl.Size = UDim2.new(1, -45, 1, 0); lbl.Position = UDim2.new(0, 15, 0, 0)
    lbl.BackgroundTransparency = 1; lbl.Text = text; lbl.TextColor3 = Color3.new(1,1,1)
    lbl.Font = "Gotham"; lbl.TextSize = 11; lbl.TextXAlignment = "Left"
    
    if isToggle then
        local st = Instance.new("TextLabel", btn)
        st.Size = UDim2.new(0, 30, 1, 0); st.Position = UDim2.new(1, -40, 0, 0)
        st.BackgroundTransparency = 1; st.Font = "GothamBold"; st.TextSize = 10
        
        local function updateVisuals()
            -- CORRECCIÓN: Usar if/else en lugar de and/or para evitar falsos positivos
            local state = false
            if stateObj then
                state = stateObj[stateKey]
            else
                state = stateKey
            end
            
            st.Text = state and "ON" or "OFF"
            -- CORRECCIÓN: Ahora marcará Rojo cuando esté en OFF
            st.TextColor3 = state and themeColor or Color3.new(1, 0, 0)
        end
        updateVisuals()
        
        btn.MouseButton1Click:Connect(function() 
            -- Alternar el estado correctamente
            if stateObj then 
                stateObj[stateKey] = not stateObj[stateKey] 
            else 
                stateKey = not stateKey 
            end
            
            updateVisuals()
            
            -- Enviar el estado correcto al callback sin errores de lógica
            if callback then 
                if stateObj then
                    callback(stateObj[stateKey])
                else
                    callback(stateKey)
                end
            end
        end)
    elseif hasArrow then
        local a = Instance.new("TextLabel", btn)
        a.Size = UDim2.new(0, 20, 1, 0); a.Position = UDim2.new(1, -25, 0, 0)
        a.BackgroundTransparency = 1; a.Text = ">"; a.TextColor3 = themeColor
        a.Font = "GothamBold"; a.TextSize = 14
        if callback then btn.MouseButton1Click:Connect(callback) end
    else
        if callback then btn.MouseButton1Click:Connect(callback) end
    end
    return btn
end


local function createSlider(text, min, max, stateObj, stateKey, parent, isFloat, callback)
    local f = Instance.new("Frame", parent)
    f.Size = UDim2.new(1, 0, 0, 45); f.BackgroundTransparency = 1
    local currentVal = stateObj and stateObj[stateKey] or stateKey
    local valStr = isFloat and string.format("%.1f", currentVal) or tostring(math.floor(currentVal))
    
    local l = Instance.new("TextLabel", f)
    l.Size = UDim2.new(1, -20, 0, 15); l.Position = UDim2.new(0, 15, 0, 5)
    l.Text = text .. ": " .. valStr; l.TextColor3 = Color3.new(1,1,1)
    l.BackgroundTransparency = 1; l.Font = "Gotham"; l.TextSize = 10; l.TextXAlignment = "Left"
    
    local b = Instance.new("Frame", f)
    b.Size = UDim2.new(1, -30, 0, 6); b.Position = UDim2.new(0, 15, 0, 25)
    b.BackgroundColor3 = Color3.fromRGB(40,40,40); b.BorderSizePixel = 0
    local fill = Instance.new("Frame", b)
    fill.Size = UDim2.new(math.clamp((currentVal-min)/(max-min), 0, 1), 0, 1, 0)
    fill.BackgroundColor3 = themeColor; fill.BorderSizePixel = 0
    
    b.InputBegan:Connect(function(input) 
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            local function update() 
                local r = math.clamp((UIS:GetMouseLocation().X - b.AbsolutePosition.X) / b.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(r, 0, 1, 0)
                local v = min + (r * (max-min))
                local finalVal = isFloat and v or math.floor(v)
                if stateObj then stateObj[stateKey] = finalVal else stateKey = finalVal end
                l.Text = text .. ": " .. (isFloat and string.format("%.1f", finalVal) or tostring(finalVal))
                if callback then callback(finalVal) end
            end
            local con = UIS.InputChanged:Connect(function(i) 
                if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then update() end 
            end)
            UIS.InputEnded:Connect(function(i) 
                if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then con:Disconnect() end 
            end)
            update()
        end 
    end)
end

local function createSub(name, list, stateObj, stateKey, parent, callback)
    local btn = createRow("⚙ "..name .. ": " .. (stateObj and stateObj[stateKey] or stateKey), parent, false, nil, nil, nil, true)
    local con = Instance.new("Frame", parent)
    con.Size = UDim2.new(1,0,0,0); con.BackgroundTransparency = 1; con.ClipsDescendants = true
    Instance.new("UIListLayout", con)
    btn.MouseButton1Click:Connect(function() con.Size = (con.Size.Y.Offset == 0) and UDim2.new(1,0,0,#list*32) or UDim2.new(1,0,0,0) end)
    for i, m in ipairs(list) do
        createRow("  ↳ "..m, con, false, nil, nil, function() 
            if stateObj then stateObj[stateKey] = m else stateKey = m end
            btn.TextLabel.Text = "⚙ "..name .. ": " .. m
            con.Size = UDim2.new(1,0,0,0)
            if callback then callback(i) end
        end, false, true)
    end
end

-- === LÓGICA DE SALA (V70) ===
local function teleportToTarget()
    if targetPlayer and targetPlayer.Character then
        local tRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        local myRoot = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        if tRoot and myRoot then myRoot.CFrame = tRoot.CFrame * CFrame.new(0, 0, 3) end
    end
end

local function stopAttack()
    local char = LP.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    local hum = char and char:FindFirstChild("Humanoid")
    flingActive, bangActive = false, false
    if root and hum then
        root.Anchored = true; hum.PlatformStand = true 
        root.Velocity, root.RotVelocity = Vector3.zero, Vector3.zero
        if root:FindFirstChild("FlingForce") then root.FlingForce:Destroy() end
        if lastPos then for i=1,5 do root.CFrame = lastPos RunService.Heartbeat:Wait() end end
        task.wait(0.1); hum.PlatformStand = false; root.Anchored = false
    end
    lastPos = nil
end

local function resetCamera()
    spectateActive = false
    local char = LP.Character
    if char and char:FindFirstChild("Humanoid") then
        Camera.CameraType = Enum.CameraType.Custom
        Camera.CameraSubject = char.Humanoid
    end
end

local function openTarget(p)
    targetPlayer = p
    pages["SALA"].Visible = false
    pages["SALA_OPTS"].Visible = true
    
    for _, v in pairs(pages["SALA_OPTS"]:GetChildren()) do if not v:IsA("UIListLayout") then v:Destroy() end end
    
    createRow("<- VOLVER A SALA", pages["SALA_OPTS"], false, nil, nil, function() 
        resetCamera(); stopAttack(); pages["SALA_OPTS"].Visible = false; pages["SALA"].Visible = true 
    end, false, true).TextLabel.TextColor3 = themeColor
    
    createRow("Espectar", pages["SALA_OPTS"], true, nil, spectateActive, function(v) spectateActive = v if not v then resetCamera() end end)
    createRow("Teletransportar", pages["SALA_OPTS"], false, nil, nil, teleportToTarget, true, true)
    
    createSub("Modo Fling", flingModes, nil, flingModes[flingModeIndex], pages["SALA_OPTS"], function(i) flingModeIndex = i end)
    createRow("Activar Fling", pages["SALA_OPTS"], true, nil, flingActive, function(v) 
        if v then lastPos = LP.Character.HumanoidRootPart.CFrame; flingActive, bangActive = true, false else stopAttack() end 
    end)
    createSlider("Fling Power", 1000, 500000, nil, flingRot, pages["SALA_OPTS"], false, function(v) flingRot = v end)
    
    createSub("Modo Bang", bangModes, nil, bangModes[modeIndex], pages["SALA_OPTS"], function(i) modeIndex = i end)
    createRow("Activar Bang", pages["SALA_OPTS"], true, nil, bangActive, function(v) 
        if v then bangActive, flingActive = true, false else stopAttack() end 
    end)
    createSlider("Bang Speed", 10, 100, nil, bangSpeed, pages["SALA_OPTS"], false, function(v) bangSpeed = v end)
    createSlider("Bang Tilt", 0, 90, nil, bangTilt, pages["SALA_OPTS"], false, function(v) bangTilt = v end)
end

local function refreshPlayers()
    for _, v in pairs(pages["SALA"]:GetChildren()) do if not v:IsA("UIListLayout") then v:Destroy() end end
    createRow("Ignorar Equipo", pages["SALA"], true, nil, ignoreTeam, function(v) ignoreTeam = v; refreshPlayers() end)
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP then
            if ignoreTeam and p.TeamColor == LP.TeamColor then continue end
            local btn = createRow(p.Name, pages["SALA"], false, nil, nil, function() openTarget(p) end, true, true)
            local colorMark = Instance.new("Frame", btn)
            colorMark.Size = UDim2.new(0, 6, 0, 6); colorMark.Position = UDim2.new(0, 5, 0.5, -3)
            colorMark.BackgroundColor3 = p.TeamColor.Color; colorMark.BorderSizePixel = 0
        end
    end
end
Players.PlayerAdded:Connect(function() task.wait(0.5); refreshPlayers() end)
Players.PlayerRemoving:Connect(refreshPlayers)
refreshPlayers()

-- === CONSTRUCCIÓN MENÚS ESP/AIM/VENTAJA ===
createRow("ESP Nombres", pages["ESP"], true, Settings, "ESP_Names")
createRow("ESP Lineas", pages["ESP"], true, Settings, "ESP_Lines")
createRow("ESP Chams", pages["ESP"], true, Settings, "ESP_Chams")
createRow("Mostrar Aliados", pages["ESP"], true, Settings, "ShowAliados")
createSlider("Distancia Max ESP", 100, 5000, Settings, "MaxDistance", pages["ESP"], false)

createRow("Activar Aimbot", pages["AIM"], true, Settings, "Aimbot")
createRow("Mostrar FOV", pages["AIM"], true, Settings, "ShowFOV")
createRow("Team Check", pages["AIM"], true, Settings, "TeamCheck")
createRow("Visible Check", pages["AIM"], true, Settings, "VisibleCheck")
createRow("Aim 360 Total", pages["AIM"], true, Settings, "Aim360Total")
createSub("Prioridad", {"Mira", "Distancia"}, Settings, "Priority", pages["AIM"])
createSub("Objetivo", {"Head", "HumanoidRootPart", "UpperTorso"}, Settings, "AimPart", pages["AIM"])
createSlider("Radio FOV", 10, 800, Settings, "FOV", pages["AIM"], false)
createSlider("Suavidad Aim", 0, 10, Settings, "Smoothing", pages["AIM"], false)

createRow("Activar Ventaja", pages["VENTAJA"], true, Settings, "VentajaEnabled")
createSub("Traer Parte", {"Cabeza", "Torso", "Todo"}, Settings, "VentajaTarget", pages["VENTAJA"])
createRow("Auto Disparo", pages["VENTAJA"], true, Settings, "AutoShoot")
createSlider("Delay Disparo", 0, 1, Settings, "AutoShootDelay", pages["VENTAJA"], true)
createSlider("Offset X", -10, 10, Settings, "OffX", pages["VENTAJA"], true)
createSlider("Offset Y", -10, 10, Settings, "OffY", pages["VENTAJA"], true)
createSlider("Offset Z", -10, 10, Settings, "OffZ", pages["VENTAJA"], true)

-- === LOOPS DE FÍSICA Y VISUALES ===
local function isVisible(part, char)
    if not Settings.VisibleCheck then return true end
    local params = RaycastParams.new(); params.FilterType = Enum.RaycastFilterType.Exclude; params.FilterDescendantsInstances = {LP.Character, char}
    return workspace:Raycast(Camera.CFrame.Position, (part.Position - Camera.CFrame.Position), params) == nil
end

RunService.RenderStepped:Connect(function()
    -- CÁMARA ESPECTAR (SALA)
    if spectateActive and targetPlayer and targetPlayer.Character then
        local tHum = targetPlayer.Character:FindFirstChild("Humanoid")
        if tHum then Camera.CameraType = Enum.CameraType.Custom Camera.CameraSubject = tHum end
    end
    
    -- LÓGICA AIM/ESP
    if not Running then return end
    updateFOVCircle()
    local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    local potTarget, lastVal = nil, math.huge

    for _, p in pairs(Players:GetPlayers()) do
        local char = p.Character
        if p ~= LP and char and char:FindFirstChild(Settings.AimPart) and char.Humanoid.Health > 0 then
            local part = char[Settings.AimPart]
            local pos, onscreen = Camera:WorldToViewportPoint(part.Position)
            local mag = (part.Position - LP.Character.HumanoidRootPart.Position).Magnitude
            local isAlly = (p.Team == LP.Team) or (p.TeamColor == LP.TeamColor)
            local isEnemy = not Settings.TeamCheck or not isAlly
            
            if (isEnemy or Settings.ShowAliados) and mag <= Settings.MaxDistance then
                local color = (p == CurrentTarget) and Color3.new(1,1,0) or ((not Settings.TeamCheck) and Color3.new(1,0,0) or (isAlly and Color3.new(0,1,0) or Color3.new(1,0,0)))
                if Settings.ESP_Names and onscreen then
                    if not nms[p] then nms[p] = Drawing.new("Text") nms[p].Center, nms[p].Outline = true, true end
                    nms[p].Visible, nms[p].Color, nms[p].Text, nms[p].Position = true, color, p.Name, Vector2.new(pos.X, pos.Y - 25)
                elseif nms[p] then nms[p].Visible = false end
                if Settings.ESP_Lines and onscreen then
                    if not lns[p] then lns[p] = Drawing.new("Line") end
                    lns[p].Visible, lns[p].Color, lns[p].From, lns[p].To = true, color, Vector2.new(center.X, Camera.ViewportSize.Y), Vector2.new(pos.X, pos.Y)
                elseif lns[p] then lns[p].Visible = false end
                if Settings.ESP_Chams then
                    if not chams[p] then chams[p] = Instance.new("Highlight", CoreGui) end
                    chams[p].Adornee, chams[p].Enabled, chams[p].FillColor = char, true, color
                elseif chams[p] then chams[p].Enabled = false end

                if isEnemy then
                    local sDist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
                    if (Settings.Aim360Total or (onscreen and sDist <= Settings.FOV)) and isVisible(part, char) then
                        local val = (Settings.Priority == "Mira") and sDist or mag
                        if val < lastVal then lastVal, potTarget = val, p end
                    end
                end
            else removeVisuals(p) end
        else removeVisuals(p) end
    end

    if CurrentTarget ~= potTarget and CurrentTarget then pcall(function() CurrentTarget.Character.Head.Anchored = false end) end
    CurrentTarget = potTarget

    if CurrentTarget and Settings.Aimbot then
        local char = CurrentTarget.Character
        Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, char[Settings.AimPart].Position), Settings.Smoothing == 0 and 1 or (1 - (Settings.Smoothing/10)))
        
        if Settings.VentajaEnabled then
            local function movePart(part) if part and part:IsA("BasePart") then part.Anchored = true part.CanCollide = false part.CFrame = LP.Character.HumanoidRootPart.CFrame * CFrame.new(Settings.OffX, Settings.OffY, Settings.OffZ) end end
            pcall(function() if Settings.VentajaTarget == "Cabeza" then movePart(char:FindFirstChild("Head")) elseif Settings.VentajaTarget == "Torso" then movePart(char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")) elseif Settings.VentajaTarget == "Todo" then for _, v in pairs(char:GetChildren()) do movePart(v) end end end)
        end
        if Settings.AutoShoot and (tick() - lastShot) >= Settings.AutoShootDelay then
            if isVisible(char[Settings.AimPart], char) then
                pcall(function() VIM:SendMouseButtonEvent(0, 0, 0, true, game, 0) task.wait(0.02) VIM:SendMouseButtonEvent(0, 0, 0, false, game, 0) end)
                lastShot = tick()
            end
        end
    end
end)

RunService.Heartbeat:Connect(function()
    -- LÓGICA FLING/BANG (SALA)
    if not targetPlayer or not targetPlayer.Character then return end
    local char, tChar = LP.Character, targetPlayer.Character
    local root, tRoot = char and char:FindFirstChild("HumanoidRootPart"), tChar:FindFirstChild("HumanoidRootPart")
    
    if root and tRoot and not root.Anchored then
        local t = tick()
        if flingActive then
            local bf = root:FindFirstChild("FlingForce") or Instance.new("BodyAngularVelocity", root)
            bf.Name = "FlingForce"; bf.MaxTorque = Vector3.new(math.huge, math.huge, math.huge); bf.AngularVelocity = Vector3.new(0, flingRot, 0)
            root.Velocity = Vector3.new(4500, 4500, 4500)
            local fMode = flingModes[flingModeIndex]
            if fMode == "GHOST" then root.CFrame = tRoot.CFrame * CFrame.new(math.sin(t*50)*1.5, 0, math.cos(t*50)*1.5)
            elseif fMode == "TORNADO" then root.CFrame = tRoot.CFrame * CFrame.new(math.sin(t*30)*2.5, 2, math.cos(t*30)*2.5)
            elseif fMode == "MISIL" then root.CFrame = tRoot.CFrame * CFrame.new(0, 0, math.sin(t*40)*3.5)
            elseif fMode == "DEBAJO" then root.CFrame = tRoot.CFrame * CFrame.new(0, -3.8, 0) end
        elseif bangActive then
            root.Velocity = Vector3.zero
            local sin = math.sin(t * bangSpeed)
            local bMode, tilt = bangModes[modeIndex], CFrame.Angles(math.rad(sin * bangTilt), 0, 0)
            if bMode == "ESPALDA" then root.CFrame = tRoot.CFrame * CFrame.new(0, 0, 1.2 + (sin * 0.4)) * tilt
            elseif bMode == "CABEZA" then local head = tChar:FindFirstChild("Head") if head then root.CFrame = CFrame.lookAt(head.Position + (head.CFrame.LookVector * 1.1), head.Position) * tilt end
            elseif bMode == "CABEZA ALTO" then local head = tChar:FindFirstChild("Head") if head then root.CFrame = CFrame.lookAt(head.Position + (head.CFrame.LookVector * 1.1) + Vector3.new(0,1.5,0), head.Position) * tilt end
            elseif bMode == "PISO (ESPALDA)" then root.CFrame = tRoot.CFrame * CFrame.new(0, -2.4, 1.2) * CFrame.Angles(math.rad(90), 0, 0) * tilt end
        end
    end
end)
